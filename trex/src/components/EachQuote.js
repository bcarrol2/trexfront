import React from 'react';

const EachQuote = (props) => {
    const list = props.quotes.map(quote => {
        quote.image_url
    })
    
    return(
        <div>
            <h1></h1>
        </div>
    )
}

export default EachQuote;